Mask Detection

This is an Object Detection Based Model that can detect if the Person is wearing Mask or not.
If the Person is Detected Without wearing a mask the the model will automatically captures his pictures and save them into the "WithoutMask_Images Folder"
